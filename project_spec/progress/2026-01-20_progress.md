# Agentromatic — Engineering Progress Log
Date: 2026-01-20  
Author: Assistant + Travis (execution of interactive steps)  
Scope: Phase 0–1 foundation + Convex initialization + beginning UI→Convex wiring

---

## 1) Executive summary
You now have:
- A working monorepo scaffold (`apps/web`, `packages/shared`, `convex`, `project_spec`)
- Shared Zod schemas/types for core entities
- Convex schema + Phase 1 CRUD functions + Phase 1 `executeWorkflow` stub action
- Convex project created and linked (interactive `npx convex dev` completed)
- Convex typecheck issues largely addressed on backend (ownership helper removed; auth helper refactored; `executeWorkflow` action updated)
- Frontend wiring to Convex started, but the web app currently fails TypeScript typecheck due to a string escaping bug in `apps/web/src/AppConvex.tsx` (see “Current blockers”)

This progress log is intended to let you restart in a new prompt with full context.

---

## 2) Specs / decisions captured
### 2.1 Inputs (existing)
- `project_spec/workflow_automation.md`
- `project_spec/workflow_automation_design_spec.md`

### 2.2 Generated “coding-ready” docs
- `project_spec/mvp_build_plan.md`
  - Locks MVP defaults: safe condition DSL, snapshotting required, sequential execution MVP, self-heal auto-apply OFF, retention defaults, tenancy defaults.
- `project_spec/adr/0001-monorepo-and-tooling.md`
- `project_spec/adr/0002-condition-language-mvp.md`
- `project_spec/ui_routes.md`
- `project_spec/api_contracts.md`
- `project_spec/convex_setup.md` (includes dev anonymous user mode guidance)

---

## 3) Repository scaffold & tooling (Phase 0)
### 3.1 Created/updated at repo root
- `package.json` set up for npm workspaces (scripts for dev/build/typecheck/etc)
- `.gitignore` (Node/Vite/Convex generated artifacts)
- `README.md` (how to run, what’s implemented, next steps)
- `.env.example` and `apps/web/.env.example`

### 3.2 Notes
- npm workspaces: `apps/*`, `packages/*`
- `packages/shared` is referenced by the web app as a local workspace dependency (version `0.0.0`).

---

## 4) Shared types package (`packages/shared`)
### 4.1 Implemented
- `packages/shared/src/schemas.ts`
  - Zod schemas + types:
    - `WorkflowSchema`, `ExecutionSchema`, `ExecutionLogEntrySchema`, `IntegrationCredentialSchema`
    - owner modeling uses `owner: { userId?: string; teamId?: string }` with “exactly one” enforced via Zod refinements
- `packages/shared/src/index.ts` exports schemas/types

### 4.2 Current caveat
- Convex tables store ownership as `userId`/`teamId` on the document rather than nested `owner`. This is fine; shared schemas currently represent a higher-level “portable” shape and may need an adapter layer when you formalize shared contracts across frontend/backend.

---

## 5) Convex backend (`convex/`) — Phase 1
### 5.1 Schema
- `convex/schema.ts` defines tables:
  - `users`, `teams`
  - `workflows`
  - `executions`
  - `executionLogs`
  - `integrationCredentials`
- Indexes for listing workflows and executions by owner and timestamps

### 5.2 Implemented modules
- `convex/workflows.ts`: list/get/create/update/setStatus/remove
- `convex/executions.ts`: listByWorkflow/get/listRecent/create/markRunning/complete
- `convex/executionLogs.ts`: list/append
- `convex/executeWorkflow.ts`: Phase 1 stub action that:
  - creates an execution (`status: running`)
  - writes `__start__` and `__end__` logs
  - completes execution as `success` (or `failed` with error)
- `convex/users.ts`: added later to support bootstrapping a user row
  - `users.bootstrap` mutation: ensures user row exists
  - `users.me` query: debug helper

### 5.3 Auth / tenancy scaffolding
- `convex/lib/auth.ts`:
  - Refactored to handle the difference between query (read-only) and mutation (read/write) DB contexts.
  - Adds optional “dev anonymous user mode”:
    - If Convex env `AGENTROMATIC_DEV_ANON_USER=true`, unauthenticated calls use externalId `"dev_anonymous"`.
    - Important: the first creation of that user row must happen in a mutation (`users.bootstrap` does that).
- Team auth is still MVP-simplified: team access is allowed only when `teams.ownerUserId === currentUserId`.

### 5.4 Removed problematic helper
- `convex/lib/ownership.ts` was deleted because:
  - It imported types that don’t exist in the current Convex server typings (`DatabaseReader` export mismatch).
  - It attempted generic constraints that don’t match Convex’s `Doc/TableNames` constraints.
  - It was not referenced by current modules (workflows/executions/logs already perform inline ownership checks).

### 5.5 Convex init status (interactive completed)
- `npx convex dev` login succeeded
- Project created: `agentromatic-com` under account `travis-burandt`
- Dev deployment created
- `convex/_generated/` exists (api + server types) and is gitignored
- A repo root `.env.local` was written by Convex with `CONVEX_URL` and `CONVEX_DEPLOYMENT` (file is treated as private and should remain uncommitted)

---

## 6) Frontend (`apps/web`) — current state
### 6.1 Web app scaffold
- Vite + React + TS scaffold exists.
- Convex client dependency was added to `apps/web/package.json`:
  - `"convex": "^1.31.5"`

### 6.2 UI wiring approach (in progress)
- Goal: build a minimal Phase 1 UI that calls:
  - `users.bootstrap` (on load)
  - `workflows.list/create/get/update/setStatus/remove`
  - `executeWorkflow.executeWorkflow` action
  - `executions.listByWorkflow/get`
  - `executionLogs.list`

### 6.3 Current files of interest
- `apps/web/src/App.tsx` should be a thin re-export:
  - intended content: `export { default } from "./AppConvex";`
  - verify file contents are correct.
- `apps/web/src/AppConvex.tsx` (new) contains the Convex-connected UI.

### 6.4 Current blocker (TypeScript)
Web app typecheck error:
- `apps/web/src/AppConvex.tsx` line ~52 (reported):
  - `return ""(unserializable)"";` or similar malformed string literal
  - This is a string escaping / generation bug in `safeJson()` fallback return value.
- Fix should be:
  - `return "\"(unserializable)\"";`
  - or `return '"(unserializable)"';`
- After fixing, rerun:
  - `npm run typecheck`

---

## 7) Known issues / risks
1. **Frontend requires `VITE_CONVEX_URL`**
   - The web app needs `apps/web/.env.local` with:
     - `VITE_CONVEX_URL=<value of CONVEX_URL from repo root .env.local>`
2. **Auth not wired yet**
   - Without Clerk (or other auth) you must temporarily enable:
     - Convex env var: `AGENTROMATIC_DEV_ANON_USER=true`
   - Then run `users.bootstrap` once to create the `"dev_anonymous"` user row.
3. **Convex action typing cycles**
   - `convex/executeWorkflow.ts` uses a cast of `api` to `any` to avoid TS inference cycles when importing generated `api` in the same module.
   - This is acceptable for Phase 1; revisit later for stronger typing.

---

## 8) What “done for Phase 1 thin slice” should look like
When the blocker is fixed and wiring is complete:
1. Start Convex (interactive terminal):
   - `npx convex dev`
2. Ensure dev anon mode (until Clerk is wired):
   - `npx convex env set AGENTROMATIC_DEV_ANON_USER true`
3. Create `apps/web/.env.local`:
   - `VITE_CONVEX_URL=...` (copy from root `.env.local` `CONVEX_URL`)
4. Start web:
   - `npm run dev`
5. In the UI:
   - Create workflow
   - View/edit JSON definition
   - Run workflow (stub)
   - View execution + logs (`__start__`, `__end__`)

---

## 9) Immediate next steps (for next prompt)
### 9.1 Unblock build
- Fix `apps/web/src/AppConvex.tsx` safeJson fallback string
- Run `npm run typecheck` to confirm clean

### 9.2 Ensure local env is set
- Create `apps/web/.env.local` with `VITE_CONVEX_URL`
- Set Convex env:
  - `AGENTROMATIC_DEV_ANON_USER=true` (dev only)

### 9.3 Verify end-to-end
- Start Convex dev and web dev server
- Use UI to:
  - create workflow
  - run workflow stub
  - view logs

### 9.4 Cleanup / polish (optional but recommended)
- Remove unused imports in `apps/web`
- Add minimal error handling UX and empty states
- Consider splitting `AppConvex.tsx` into smaller components once stable

---

## 10) Command recap
From repo root:

Run web:
```/dev/null/sh#L1-1
npm run dev
```

Typecheck:
```/dev/null/sh#L1-1
npm run typecheck
```

Convex dev (interactive):
```/dev/null/sh#L1-1
npx convex dev
```

Enable dev anon mode (temporary, dev only):
```/dev/null/sh#L1-1
npx convex env set AGENTROMATIC_DEV_ANON_USER true
```

---

## 11) Files changed/added highlights (non-exhaustive)
- `project_spec/mvp_build_plan.md`
- `project_spec/api_contracts.md`
- `project_spec/ui_routes.md`
- `project_spec/convex_setup.md`
- `project_spec/adr/0001-monorepo-and-tooling.md`
- `project_spec/adr/0002-condition-language-mvp.md`
- `packages/shared/*`
- `convex/schema.ts`
- `convex/workflows.ts`
- `convex/executions.ts` (validators cleanup; keep `ExecutionStatus`)
- `convex/executionLogs.ts`
- `convex/executeWorkflow.ts` (type inference cycle workaround)
- `convex/users.ts` (bootstrap/me)
- `convex/lib/auth.ts` (query vs mutation DB handling + dev anon mode)
- `apps/web/src/AppConvex.tsx` (Convex UI — currently has a string bug)
- `apps/web/src/App.tsx` intended to re-export `AppConvex` (verify)

---