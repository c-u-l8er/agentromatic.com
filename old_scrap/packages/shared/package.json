{
  "name": "@agentromatic/shared",
  "private": true,
  "version": "0.0.0",
  "description": "Shared schemas, types, and node definitions for Agentromatic",
  "license": "UNLICENSED",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist",
    "src"
  ],
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(shared) no lint configured yet\"",
    "test": "echo \"(shared) no tests configured yet\"",
    "format": "echo \"(shared) no formatter configured yet\"",
    "format:check": "echo \"(shared) no formatter configured yet\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
